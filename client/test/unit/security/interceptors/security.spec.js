describe('Security interceptor', function() {
  var expect = chai.expect;

  var queue, 
    interceptor;

  beforeEach(
    module(
      // TODO : set up the module dependencies
    ));

  beforeEach(inject(function($injector) {
    // TODO : inject the interceptor and the retryQueue
  }));

  // TODO : verify it accepts a response object and returns a promise
  // TODO : verify it intercepts 401 error responses and adds it to the retry queue
  // TODO : verify it intercepts 403 error responses and adds it to the retry queue
  
});
