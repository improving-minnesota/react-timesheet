describe('Timeunit Row Component: ', function () {

  var TimeunitRow;

  beforeEach(function () {
    // TimeunitRow = require('./timeunit.row');
  });

  it('should instantiate the TimeunitRow', function () {
    // expect(TimeunitRow).toBeDefined();
  });
});
