jest.dontMock('./timeunit.edit');

describe('Timeunit Edit Component: ', function () {

  var TimeunitEdit;

  beforeEach(function () {
    // TimeunitEdit = require('./timeunit.edit');
  });

  it('should instantiate the TimeunitEdit', function () {
    // expect(TimeunitEdit).toBeDefined();
  });
});
