jest.dontMock('./project.detail');

describe('Project Detail Component: ', function () {

  var ProjectDetail;

  beforeEach(function () {
    // ProjectDetail = require('./project.detail');
  });

  it('should instantiate the ProjectDetail', function () {
    // expect(ProjectDetail).toBeDefined();
  });
});
