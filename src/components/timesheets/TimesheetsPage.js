import React, { Component, PropTypes } from 'react'

export class TimesheetsPage extends Component {
  render() {
    return (
      <div>
        This is the timesheets page
      </div>
    )
  }
}

TimesheetsPage.propTypes = {
};

export default TimesheetsPage
